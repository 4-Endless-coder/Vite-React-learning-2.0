const User =({user}) => {
    
    return (
        <div>
            <hr />
            <h2>Name: {user.name}</h2>
            <h2>Name: {user.age}</h2>
            <h2>Name: {user.email}</h2>
        </div>
    );
};

export default User;